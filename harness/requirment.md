## i want to create web ui to generate pinot schema json and pinot table json for user:
**1.** user can use ui to select/fill in different pinot table configuration

**2.** user can add multiple columns for the table

**3.** web ui should generate valid json for table and schemaa based on user input, those json should be able to use in pinot api directly to create table and schema

**4.** supported pinot table andn schema configuration are in references folder

**5.** it should be a purely client side web ui, no backend needed and should use react/refine framework and typescript